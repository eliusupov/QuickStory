# Deny-list: node paths the merge must REFUSE TO WRITE even though they are absent from the
# target. The additive-only gate cannot catch these — it only guards against overwriting what
# already exists, and every path below is a v84 *addition*, so no collision fires and the merge
# writes it silently. Produced by ticket 03c; evidence in COLLISION-TRIAGE.md.
#
# ENFORCED since ticket 03e: pass this file as `WzMerge merge|xml --deny <file>`. Both
# subcommands REQUIRE the flag — there is no run without a deny-list, including dry runs, since
# a dry run is what the operator reads before deciding.
#
# FORMAT — deliberately identical to COLLISION-FORCE.txt so one parser serves both:
#   <path>\t# <reason>
# - blank lines and lines whose first non-space char is '#' are ignored
# - a listed path is a deny ROOT: nothing at or beneath it is written. A manifest row that is a
#   copy root CONTAINING a deny root is refused too — a partial write is not possible.
# - NO WILDCARD SYNTAX IS NEEDED. Root semantics already cover every case below; the 36
#   at-risk MonsterBook slots are reached by denying 17 `reward` parents, not 36 globs.
#
# Precedence, if both lists are ever supplied: deny wins. A path in both is an operator error
# and should be a hard exit, not a silent resolution.
#
# 192 roots. (28 from 03c, +12 from 03g/ticket 08, +116 from 03h/ticket 09, +32 from 03i:
# 2 glove rows in Hazard 2d, 2 Item.wz reward slots in Hazard 2e, and 28 live-map container roots
# in Hazard 5; +4 from ticket 43, closing the Hazard 1 floor — see below.)

# --- Hazard 1: server-owned id range -----------------------------------------------------
# PlayerNPC.java:66-67 declares 9901910-9906599 a range the server allocates from AT RUNTIME
# for player-rank NPCs. v84 adds fixed world placements on the first ten of it (9901910/0 =
# 100030301, Nexon's Lv.200 fame NPC). Live has none of these, so the gate is silent. Writing
# them puts a static NPC on ids the server hands out dynamically.
# NOTE: refusing the ten Npc.wz/99019xx.img images — which the gate already does — does NOT
# cover this. Different file, different mechanism, and this one is not a collision at all.
Etc.wz/NpcLocation.img/9901910	# server-allocated id range, PlayerNPC.java:66
Etc.wz/NpcLocation.img/9901911	# server-allocated id range, PlayerNPC.java:66
Etc.wz/NpcLocation.img/9901912	# server-allocated id range, PlayerNPC.java:66
Etc.wz/NpcLocation.img/9901913	# server-allocated id range, PlayerNPC.java:66
Etc.wz/NpcLocation.img/9901914	# server-allocated id range, PlayerNPC.java:66
Etc.wz/NpcLocation.img/9901915	# server-allocated id range, PlayerNPC.java:66
Etc.wz/NpcLocation.img/9901916	# server-allocated id range, PlayerNPC.java:66
Etc.wz/NpcLocation.img/9901917	# server-allocated id range, PlayerNPC.java:66
Etc.wz/NpcLocation.img/9901918	# server-allocated id range, PlayerNPC.java:66
Etc.wz/NpcLocation.img/9901919	# server-allocated id range, PlayerNPC.java:66

# --- Hazard 1b: the SAME range, BELOW the 9901910 floor (ticket 43) ----------------------
# The ten rows above encode the range as "9901910 and up" because that is where 03c happened to
# find v84 rows. THE RANGE IS WIDER THAN ITS FLOOR AND ALWAYS WAS. Verified in code, not in a
# comment:
#   constants/id/NpcId.java:38                PLAYER_NPC_BASE = 9900000
#   server/life/PlayerNPC.java:321-323        branchSid = PLAYER_NPC_BASE + branch*100,
#                                             nextBranchSid = branchSid + (branch < 26 ? 100 : 400)
#   constants/game/GameConstants.java:386     non-hall-of-fame branch = 26 + 4*(mapid/100000000),
#                                             so the highest branch is 62 (mapid 990000000)
#                                             -> 9900000 + 6200 + 400 = 9906600 exclusive.
# The allocator therefore hands out 9900000-9906599, plus the fixed 9977777
# (NpcId.CUSTOM_DEV). PlayerNPC.java:66-67's own comment says the same thing; the code confirms
# both ends of it. Anything Nexon ships anywhere inside that band is a fixed placement or a
# fixed name landing on ids this server assigns to real players' rank NPCs at runtime.
#
# The 149 PlayerNPC rows the ticket-42 audit found in 9901000-9901849 are Map.wz `life` entries
# and are NOT listed here: a life slot has no id in its path, only an index, so a deny root
# cannot name one. Those are refused structurally instead, by the positional-array gate (4.4) —
# and that refusal must be left standing rather than forced. What IS listable is every path in
# the band that names the id, and this is all of them the pinned v84 source can offer:
# `grep -rn '99[0-9]\{5\}' docs/wz-baseline/add-list/` returns 58 rows. 17 are out-of-band ids
# that only LOOK like the band (String.wz/Mob.img/9990000-9990015, Mob.wz/9999999.img). 10 are
# the NpcLocation rows denied above. 20 more — Npc.wz/990191x.img and String.wz/Npc.img/990191x
# — are in the band but name whole nodes this tree already has, so the additive gate refuses
# them on its own; they are left to it rather than duplicated here. The remaining 11 — on ids
# 9900000, 9900001 and 9901000 — sit BELOW 9901910 and were uncovered by anything until now.
# All 11 are INTERIOR writes into nodes this server already has (Npc.wz/9900000.img has `reg`
# but not `reg/name1..3`; String.wz/Npc.img/9901000 has n0/n1/d0 but not `name`), which is the
# 4.5 shape the additive gate is blind to by construction: no collision fires, conflicts.txt
# stays empty, and the merge quietly edits a PlayerNPC slot.
# Re-run that grep if the v84 source is ever re-pinned; the add-list is the enumeration.
Npc.wz/9900000.img	# PlayerNPC allocator band 9900000-9906599, NpcId.java:38 + PlayerNPC.java:321
Npc.wz/9900001.img	# PlayerNPC allocator band 9900000-9906599, NpcId.java:38 + PlayerNPC.java:321
Npc.wz/9901000.img	# PlayerNPC allocator band 9900000-9906599, NpcId.java:38 + PlayerNPC.java:321
String.wz/Npc.img/9901000	# PlayerNPC allocator band; v84 adds /name to a live rank-NPC slot

# --- Hazard 2: positional arrays cannot be merged slot-by-slot ---------------------------
# `reward/<n>` is the n-th slot of a monster-book drop list, not an identity. Cosmic rewrote
# these tables (MonsterBook.img 271350 -> 295409 bytes, the largest live-side edit). Of 689
# v84 reward slots on the add-list, 653 collide and are refused — leaving 36 that get appended
# into these 17 Cosmic lists. Result: Cosmic's entries at indices 0-22, Nexon's at 23-28, a
# list neither vendor ever shipped. There is no partial answer; deny the whole `reward` node.
# GENERAL RULE for the procedure: any manifest row that is one slot of a positional array is
# unsafe to merge piecemeal. MonsterBook will not be the last one.
String.wz/MonsterBook.img/3100101/reward	# positional array, 36 slots across these 17 mobs would splice
String.wz/MonsterBook.img/3110301/reward	# positional array
String.wz/MonsterBook.img/3210203/reward	# positional array
String.wz/MonsterBook.img/3210206/reward	# positional array
String.wz/MonsterBook.img/3210207/reward	# positional array
String.wz/MonsterBook.img/4230125/reward	# positional array
String.wz/MonsterBook.img/4230400/reward	# positional array
String.wz/MonsterBook.img/4230503/reward	# positional array
String.wz/MonsterBook.img/4230504/reward	# positional array
String.wz/MonsterBook.img/4250001/reward	# positional array
String.wz/MonsterBook.img/5110300/reward	# positional array
String.wz/MonsterBook.img/5120501/reward	# positional array
String.wz/MonsterBook.img/6130202/reward	# positional array
String.wz/MonsterBook.img/7130102/reward	# positional array
String.wz/MonsterBook.img/8140100/reward	# positional array
String.wz/MonsterBook.img/8140701/reward	# positional array
String.wz/MonsterBook.img/8810018/reward	# positional array

# --- Hazard 2b: the same hazard, on portals players currently use -------------------------
# Ticket 08. `add-list/Map.txt` offers 18 rows that write into a `portal`/`obj` array on a map
# the LIVE CLIENT ALREADY HAS. Six are pure appends and were merged; these twelve are not —
# v84 reordered or inserted entries, so the same index names a different portal in each tree.
# Ten are the shape `<array>/<n>/<field>` where `<n>` already exists in the target, which the
# additive gate passes (parent exists, leaf does not) and then writes onto the wrong sibling.
# Every live/v84 pair below was dumped index by index; ROUTE-ROWS.md in `08/` records the
# comparison and the cost of refusing them (three staged areas have no route in).
# SINCE TICKET 03g THE TOOL ALSO REFUSES THESE STRUCTURALLY — a write into a container whose
# children are ONE CONSECUTIVE RUN of integers must be a pure append onto the end whose content
# is not already in the array, and the refusal is reported as `POSITIONAL ARRAY:` rather than as
# a collision. (03g demanded the run start at 0; 03i does not — see Hazard 2d.)
# These rows stay listed anyway: they are a decision about data, and a deny row says WHY.
Map.wz/Map/Map1/106010101.img/portal/5/horizontalImpact	# positional array: live slot 5 is out00 (-> 106010100), v84's is in00; would move the exit portal
Map.wz/Map/Map1/106010101.img/portal/5/script	# hand-authored, not merged: ticket 54 wrote evanGolemDoor.js and took v84's whole node (pt/tm/tn/script) by hand
Map.wz/Map/Map1/106010102.img/portal/4/horizontalImpact	# positional array: v84 moved out00 to index 3, so live 4=in04 but v84 4=in03 — one portal off
Map.wz/Map/Map1/106010102.img/portal/5/horizontalImpact	# positional array: live 5=in05, v84 5=in04
Map.wz/Map/Map1/106010102.img/portal/6/horizontalImpact	# positional array: live 6=in06, v84 6=in05
Map.wz/Map/Map1/106010102.img/portal/7/horizontalImpact	# positional array: live 7=out00, v84 7=in06
Map.wz/Map/Map2/220000300.img/portal/4/horizontalImpact	# positional array: v84 inserted scr00 at 4; live 4 is the hidden town point h000
Map.wz/Map/Map2/220000300.img/portal/4/script	# positional array: would attach enterBlackFrog to hidden town portal h000. This row IS v84's Frog House entrance and is simply unusable at this index
Map.wz/Map/Map2/220000300.img/portal/6/image	# positional array: live 6=west00 (-> 220000400), v84 6=h001
Map.wz/Map/Map2/220000300.img/portal/15	# positional array: appends a DUPLICATE in06 — v84 has 16 portals only because it inserted scr00 at 4, so its slot 15 is byte-identical to live's slot 14
Map.wz/Map/Map2/220011000.img/portal/4/horizontalImpact	# positional array: live slot 4 is the WORKING portal into Ludibrium Toy Factory (in00 pt=2 -> 220011001)
Map.wz/Map/Map2/220011000.img/portal/4/script	# hand-authored, not merged: ticket 54 wrote enterBlackBC.js and took v84's whole node (pt/tm/tn/script) by hand

# --- Hazard 2d: the same hazard, on an array that does not start at 0 ----------------------
# Ticket 03i, found by ticket 16's regression pass. `Character.wz/Glove/01082262.img` is the
# live client's "Dragon Master's Proof", shipped with Ezorsia's own art. 03g refused six rows on
# this image on exactly the reasoning above. Two more rows of the SAME SHAPE landed anyway,
# because 03g's rule demanded the integer run start at 0 and these two containers do not:
#
#   swingT2  live {1: rGlove 5x6, 2: lGlove 12x8}   v84 {1: rGlove 7x5, 2: rGlove 12x9}
#            merged -> slot 2 held Ezorsia's lGlove 12x8 AND v84's rGlove 12x9, one frame
#   swingO3  live {1: rGlove 6x5}                   v84 {0: lGlove 7x5 + rGlove 47x9, 1: ...}
#            merged -> {0: v84's pair, 1: Ezorsia's} — two frames from two art sets, and a
#            47x9 sprite against a 6x5 one, so these are very unlikely to be the same animation
#
# 03i widened the rule to any consecutive run (`ArrayRange`), and both now refuse structurally:
# swingT2's as an interior write, swingO3's as a PREPEND (slot 0 below a run that starts at 1).
# They are listed here for the same reason 08's twelve are — the gate records a shape, a deny
# row records the decision. Cost of refusing: four animation frames of one cosmetic equip keep
# Ezorsia's art, which is what the other six refusals already chose for the same item.
Character.wz/Glove/01082262.img/swingT2/2/rGlove	# positional array (03i): live swingT2 is {1,2}; writes v84's rGlove 12x9 into the slot holding Ezorsia's lGlove 12x8
Character.wz/Glove/01082262.img/swingO3/0	# positional array (03i): live swingO3 is {1}; slot 0 is a PREPEND from a differently-numbered v84 animation (47x9 rGlove vs the live 6x5)

# --- Hazard 2e: a content-identical append the XML side CANNOT see ------------------------
# Ticket 03i, at 03j's request. These two rows are the `Item.wz` twin of `220000300.img/portal/15`:
# the live box holds 43 reward slots `0..42`, v84 has 44, and v84's slot 43 (item `2020014`) is
# CONTENT-IDENTICAL to the live client's slot 16. The arrays diverged — Ezorsia rewrote the table —
# so the "append" does not add an item, it DOUBLES that item's drop weight: `getItemReward` sums
# `prob` across every child, and both slots carry the same one.
#
# WHY THESE NEED A DENY ROW AND 08's PORTALS DO NOT. The binary gate already refuses them (it
# digests both slots and compares, `POSITIONAL ARRAY: ... content-identical`, measured in the
# composed run). `WzMerge xml` CANNOT: it is a line-text scan with no decoded nodes to compare, so
# it sees a pure append by index and writes it — which is exactly what happened, and 03j had to
# revert both from `wz/Item.wz/Consume/0202.img.xml` by hand. THE ROWS ARE STILL ON
# `composed/Item.paths.txt`, so without these two lines the next `xml` run silently re-applies
# them and 03j's fix is gone. This is the gap 4.4's closing paragraph describes; the deny-list is
# the only thing that closes it, because one deny-list serves both subcommands.
Item.wz/Consume/0202.img/02022503/reward/43	# 03i/03j: v84 slot 43 is content-identical to live slot 16 (item 2020014); appending it doubles that item's prob. The XML side cannot detect this — deny is what stops it.
Item.wz/Consume/0202.img/02022514/reward/43	# 03i/03j: same box, same duplicate slot, same effect

# --- Hazard 3: partial merge of one entity ------------------------------------------------
# v84 rebuilt this NPC around UOL links (Npc.txt:7 — 60238 -> 21146 bytes, it shrank by two
# thirds). 24 of its nodes are refused with `unsupported shape: parent=WzUOLProperty`, so the
# rest merge and the NPC ends up half-v83 half-v84 — worse than either whole version. Denying
# the image keeps it wholly v83, which is a state that at least works.
# This one is the OPERATOR'S CALL, not a finding: it trades a known-bad partial for a known-old
# whole. Remove the row if the tool learns to write through a UOL parent.
Npc.wz/9000021.img	# 24 UOL-shape refusals leave this NPC partially merged; deny keeps it whole

# --- Hazard 2c: the same hazard again, writing INTO live quests ---------------------------
# Ticket 09, folded in by 03h. `add-list/Quest.txt` is 924 copy roots; 792 are whole new quest
# nodes and 131 of the remaining 132 write into content the live client already ships. (03i: the
# 132nd is `Quest.wz/PQuestSearch.img`, a whole NEW image — it was filed under this heading and it
# does not belong to it. It is still unclaimed; see the closing note.) 09 refused all 132
# by leaving them off its path list, which protects 09 and nothing else — these rows sit in the
# add-list forever and the next ticket to open Quest.wz meets them fresh. Measured row by row,
# both sides dumped, in `09/DEEP-ROWS.md`.
#
# WHAT THE TOOL ALREADY DOES, so an operator knows what these rows are and are not for: all 123
# `Check.img/<id>/<step>/<field>` rows below are ALSO refused structurally by the positional-array
# gate (4.4), because `Check.img/<id>` holds exactly `0` and `1` and the row descends THROUGH an
# existing slot. That was verified against these exact rows (`added 9, refused 123`, exit 3) —
# 09 predicted an index-keyed check would miss all of them and it does not. The rows are listed
# anyway for the same reason 08's twelve are: a deny row records the DECISION and the reason,
# and the gate records only the shape.
#
# ROOT SEMANTICS: denied at `<id>/0`, the start block, not at the leaf. One root covers every
# field v84 adds to that block, so the twelve date rows on 2208-2211 collapse to four roots and
# 126 add-list rows are covered by 116. Nothing this project wants ever writes into the start
# block of a quest the live client already ships.

Quest.wz/Check.img/28162/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28163/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28164/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28165/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28166/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28167/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28168/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28169/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28170/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28171/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28172/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28173/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28174/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28175/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28176/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28177/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28178/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28179/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28180/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28181/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28182/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28183/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28184/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28185/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28186/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28187/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28188/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28189/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28190/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28191/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28192/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28193/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28194/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28195/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28196/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28197/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28198/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28199/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28200/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28201/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28202/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28203/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28204/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28205/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28206/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28207/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28208/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28209/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28210/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28211/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28212/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28213/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28214/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28215/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28216/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28217/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28218/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28219/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28220/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28221/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28222/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28223/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28224/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28225/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28226/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28227/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28228/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28229/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28230/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28231/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28232/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28233/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28234/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28235/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28236/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28237/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28238/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28239/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28240/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28241/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28242/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28243/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28244/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28245/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28246/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28247/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28248/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28249/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28250/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28251/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28252/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28253/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28254/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28255/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28256/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28257/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28258/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28259/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28260/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28261/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28262/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28263/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28264/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28265/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28266/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28282/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28283/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40
Quest.wz/Check.img/28325/0	# ticket 09: v84 adds lvmax=40 here — MAX_LEVEL start requirement, caps a working v83 quest at Lv.40

# The 13 date rows: v84 gives five currently-ungated, currently-working quests a start/end
# window that expired years ago. `EndDateRequirement.check` compares `end` against the wall
# clock, so merging these turns five working quests off permanently.
Quest.wz/Check.img/2208/0	# ticket 09: v84 adds start=200801010000 end=200801020000 interval=1440 — a 24h window in Jan 2008 on a working, ungated quest (NPC 1092011)
Quest.wz/Check.img/2209/0	# ticket 09: same 24h Jan-2008 window on a working, ungated quest
Quest.wz/Check.img/2210/0	# ticket 09: same 24h Jan-2008 window on a working, ungated quest
Quest.wz/Check.img/2211/0	# ticket 09: same 24h Jan-2008 window on a working, ungated quest
Quest.wz/Check.img/3845/0	# ticket 09: v84 adds end=2010010100 to a working, ungated Lv.60-80 quest (NPC 2092001)

# The two inert ones. Both quests are already date-dead in the live client, so these change
# nothing — refused anyway, because "inert" is the argument ticket 08's enterDollcave.js
# mistake was made on and neither row buys anything.
Quest.wz/Check.img/10109/0	# ticket 09: v84 adds interval=1440 to an already date-dead quest — inert, no benefit
Quest.wz/Check.img/9260/0	# ticket 09: v84 adds dayByDay=1 to an already date-dead quest — inert, no benefit

# --- Hazard 4: a container the two trees PARTITION differently -----------------------------
# THE ONE SHAPE NO STRUCTURAL CHECK IN THIS TOOL CATCHES, and the deny-list is the only thing
# standing in front of it. Ticket 09, `09/DEEP-ROWS.md`.
#
#   live  Exclusive.img/medal : 29000 29001 29002 29003 29300 29301 29302 29303 29304
#                               29400 29500 29501 29502 29503        (one NAMED group, 14 ids)
#   v84   Exclusive.img/0     : 10415 10417 10418 10419 10420
#         Exclusive.img/1     : 29002 29500
#         Exclusive.img/2     : 29300 29301 29302 29303 29304        (three NUMERIC groups)
#
# v84 REPLACED `medal` with numeric groups and re-partitioned the ids. `0`/`1`/`2` are genuine
# additions — the target has no child by those names — so no collision fires, the positional
# gate sees a container whose only child is `medal` and therefore not an array, and the merge
# writes an image holding `medal` AND `0`/`1`/`2`, with seven ids (29002, 29500, 29300-29304)
# in two mutually-exclusive groups at once. Neither vendor ever shipped that.
#
# It is a SEMANTIC replacement, and no depth, index or subset rule sees it — only dumping both
# sides does. A heuristic that guessed at it (say, "refuse integer children added to a container
# that has only named ones") would refuse legitimate content elsewhere and teach an operator to
# override, which is worse than this documented gap. Denied at the IMAGE, not at the three
# groups: the statement being made is that this image cannot be merged piecemeal at all.
# Nothing in src/ reads Exclusive.img (grepped, zero hits), so the cost is that v84's new
# 10415-10420 group is unenforced client-side, which beats a self-contradicting one.
Quest.wz/Exclusive.img	# ticket 09: live groups medals under the NAME `medal`, v84 under `0`/`1`/`2` — merging either partition into the other duplicates 7 ids across mutually-exclusive groups. Not mergeable piecemeal; deny the image.

# NOT DENIED, deliberately, and recorded here so the next reader does not think they were
# missed. Six of 09's 132 rows are absent from this file because they are not hazards:
#   Quest.wz/QuestInfo.img/1008/demandSummary, 9260/{demandSummary,rewardSummary},
#   {20012,20311}/type   — client-side display text on quests 09 did not own. `Quest`'s
#     constructor never reads `type`; demandSummary/rewardSummary have no server reader at
#     all. 09 refused them for consistency, not for cost.
#     03i, correcting an overstatement: "re-adding them is harmless" holds for the four
#     summary rows, which are display strings on quests that have none today. It does NOT hold
#     unqualified for `{20012,20311}/type`: server-inert, yes — verified — but `type` is the
#     client's quest-TAB categorisation, so adding it MOVES a live quest between tabs in the
#     quest window. Harmless to the server, visible to the player. Decide it, do not skim it.
#   Quest.wz/PQuestSearch.img — a whole new image indexing a party-search window the v83
#     client does not have and the server never opens. Unclaimed, not unsafe.
# A deny-list that carries non-hazards is a deny-list operators learn to skim.

# --- Hazard 5: v84 content written INTO maps the live client already has ------------------
# Ticket 03i, from ticket 16's §7. `add-list/Map.txt` holds 413 rows that write BELOW a
# `<id>.img` — into a map, not a new one — and every one of those 413 maps exists in the live
# client. Ticket 08 examined 18 of them (its own areas) and split them 6 merge / 12 deny
# (Hazard 2b). The other 395 had never been read by anyone. (§7 says 389; the arithmetic there
# subtracted 08's 6 merged rows twice. 413 - 6 merged - 12 denied = 395.)
#
# All 395 were run through the corrected gate against the live client
# (`merge <v84>\Map.wz <pre>\Map.wz - <the 395> ... --deny`, exit 3, `added 179, refused 216`):
#
#   216  REFUSED STRUCTURALLY by the positional-array gate — 200 interior writes into an
#        occupied slot, 14 appends whose content the array already holds, 2 prepends. Not
#        listed here: the gate stops them by shape and always will.
#        14 of the 216 are refused ONLY because of 03i's widened rule — every one a
#        `foothold/<n>` container, which v84 numbers from 1, not 0. That is the single
#        biggest practical effect of this ticket and it is in a file no ticket has finished.
#   179  the gate calls a pure append. Of those, 119 are scenery — `<layer>/tile/<n>`,
#        `<layer>/obj/<n>`, `miniMap`, two `<layer>/info/tS*` — and are left unclaimed and
#        undenied, exactly as they were. They draw; they do not act.
#    55  are the shape NOTHING STRUCTURAL CAN CATCH: a genuine append onto a live map that
#        CHANGES WHAT THE MAP DOES. Those 55 are denied below, as 28 container roots.
#
# WHY A CONTAINER ROOT and not 55 rows: the statement is not "not these 55 slots", it is "no
# ticket adds an NPC / reactor / portal / foothold to THIS live map without deciding to". A
# root also refuses a manifest row that contains it, which is what should happen.
#
# NONE of these 28 roots touches a row on any composed list (checked mechanically, all eleven
# files), and none of them is one of 08's six merged appends — those are on 200080600 and
# 251010403, which do not appear here.

# `life/<n>` on a live map. All 11 are `type=n` NPC placements, no mobs, so no spawn table
# moves — but 4 of the 5 npc ids have NO SPRITE in the live client (1011101, 1022106, 1022107,
# 2030015 are absent from live Npc.wz; only 9901000 is present), so merging these puts broken
# NPCs into Henesys, Ellinia, the Ellinia tree dungeon, Kerning and Herb Town. Ticket 16 §4
# states "zero life nodes were added to any map the live client already had"; these are the
# rows that would falsify it.
Map.wz/Map/Map1/100000100.img/life	# 03i: v84 places npc 1011101 in Henesys Hunting Ground I; no sprite in live Npc.wz
Map.wz/Map/Map1/101030000.img/life	# 03i: v84 places npc 1022107 in the Ellinia tree dungeon; no sprite in live Npc.wz
Map.wz/Map/Map1/101030100.img/life	# 03i: v84 places npc 1022107; no sprite in live Npc.wz
Map.wz/Map/Map1/101030200.img/life	# 03i: v84 places npc 1022107; no sprite in live Npc.wz
Map.wz/Map/Map1/101030300.img/life	# 03i: v84 places npc 1022107; no sprite in live Npc.wz
Map.wz/Map/Map1/101030400.img/life	# 03i: v84 places npc 1022107; no sprite in live Npc.wz
Map.wz/Map/Map1/102000003.img/life	# 03i: v84 places npc 9901000 (present in live) into a live Perion map — an NPC appears where players have none today
Map.wz/Map/Map1/106000000.img/life	# 03i: v84 places npc 1022106 in Kerning City; no sprite in live Npc.wz
Map.wz/Map/Map1/106000100.img/life	# 03i: v84 places npc 1022106; no sprite in live Npc.wz
Map.wz/Map/Map1/106000200.img/life	# 03i: v84 places npc 1022106; no sprite in live Npc.wz
Map.wz/Map/Map2/211040400.img/life	# 03i: v84 places npc 2030015 in El Nath; no sprite in live Npc.wz

# `reactor/<n>` on a live map. 30 rows, 7 maps. A reactor is a clickable world object with
# server-side state and drops; adding one to a map players use today is a content change, not
# a render change. 109090300 (Ola Ola area) takes 18 of them in one block.
Map.wz/Map/Map1/109090300.img/reactor	# 03i: 18 v84 reactors appended onto a live map's reactor list
Map.wz/Map/Map2/230010400.img/reactor	# 03i: v84 reactor appended onto a live Aquarium map
Map.wz/Map/Map2/230020000.img/reactor	# 03i: 2 v84 reactors appended onto a live map
Map.wz/Map/Map2/230040000.img/reactor	# 03i: 2 v84 reactors appended onto a live map
Map.wz/Map/Map2/230040100.img/reactor	# 03i: 2 v84 reactors appended onto a live map
Map.wz/Map/Map2/230040200.img/reactor	# 03i: 3 v84 reactors appended onto a live map
Map.wz/Map/Map2/230040400.img/reactor	# 03i: 2 v84 reactors appended onto a live map

# `portal/<n>` on a live map. Same class 08 measured, only these are pure appends by index so
# the gate passes them. A portal is a route: it either goes somewhere the player can now reach
# or somewhere that does not exist. 109090000's five are `pt=0` spawn points (`pn=sp`,
# `tm=999999999`), which change where an entering player LANDS.
Map.wz/Map/Map1/109090000.img/portal	# 03i: 5 v84 spawn points appended to a live map — changes where players arrive
Map.wz/Map/Map2/221000000.img/portal	# 03i: v84 portal appended to a live Omega Sector map
Map.wz/Map/Map2/222000000.img/portal	# 03i: v84 portal appended to a live Korean Folk Town map
Map.wz/Map/Map2/240000000.img/portal	# 03i: v84 portal appended to a live Leafre map

# `foothold` on a live map. Read by the server (MapFactory.java:197) as well as the client:
# this is collision geometry, and a group appended onto a live map changes where players stand.
Map.wz/Map/Map2/220011001.img/foothold	# 03i: appends a whole foothold GROUP to a live Ludibrium Toy Factory map

# `user` / `ladderRope` on a live map. Neither is read by this server (grepped: no
# `getChildByPath("user")`, no "ladderRope" in src/main/java), so these are client-side —
# a climbable rope that appears out of nowhere, and four map-entry condition blocks.
Map.wz/Map/Map1/105100300.img/user	# 03i: v84 map-entry condition block on a live Sleepywood map; not read by this server, client-side only
Map.wz/Map/Map1/105100301.img/user	# 03i: as above
Map.wz/Map/Map1/105100400.img/user	# 03i: as above
Map.wz/Map/Map1/105100401.img/user	# 03i: as above
Map.wz/Map/Map1/120000105.img/ladderRope	# 03i: appends a climbable rope to a live map

# NOT DENIED from the 395, deliberately, same doctrine as the note above:
#   - the 119 scenery appends (`<layer>/tile/<n>`, `<layer>/obj/<n>`, `miniMap`, two
#     `<layer>/info/tS*`). They are unclaimed, and the honest description of them is
#     "cosmetic, unread". Anyone merging them still owes the tile/obj asset check of 5.3.
#   - `Map/Map2/220011001.img/info/{swim,fly,noMapCmd,onFirstUserEnter,onUserEnter}`.
#     Every one of v84's values is 0 or the empty string, and Cosmic already normalises an
#     empty `onUserEnter`/`onFirstUserEnter` back to the mapid (MapFactory.java:153-157), so
#     writing them changes nothing on this server. Verified, not assumed. If a later v84 tree
#     carries a non-empty script name here, this line stops being true.

# ticket 28 - triage of v84's 110 absent Commodity slots, by SN
# 0 slots carry an SN no server row uses -> safe to add
# 0 slots carry an SN a server row ALREADY uses -> adding them
#   makes CashItemFactory's put(SN,..) overwrite an existing row. REFUSED.


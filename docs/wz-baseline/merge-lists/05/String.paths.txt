# Ticket 05 - v84 mounts rideable. String.wz merge path list (AUTHORITATIVE DELIVERABLE).
# 7 rows, ALL of them --force rows. Every one is on COLLISION-FORCE.txt already; none is a
# plain addition, so this list is USELESS without --force <lists>\COLLISION-FORCE.txt.
#
# These are exactly the 7 of COLLISION-FORCE.txt's 37 rows that ticket 04 excluded from its own
# String.paths.txt and handed to 05. 04 forced the other 30. Together 30 + 7 = 37: the whole
# force list is consumed, none twice.
#
# Eqp/Taming x6 - Evan's Mir and its saddles. The sprites shipped in v83; only the NAMES are new,
#   which is why a presence-only Character.wz diff cannot see them and why additive-only cannot
#   repair them: the ids already exist locally reading the literal "MISSING NAME"/"MISSING INFO".
#     1902040 Stage 1 Dragon        1912033 Stage 1 Dragon Saddle
#     1902041 Stage 2 Dragon        1912034 Stage 2 Dragon Saddle
#     1902042 Stage 3 Dragon        1912035 Stage 3 Dragon Saddle
#   NARROWED (ticket 03k) to Eqp/Taming/<id>/name. A force row is a ROOT, and v84's node for these
#   six carries ONLY `name` while the live client's carries `name` AND `desc`, so the id-level row
#   replaced the node and deleted the live `desc` - 6 of the 18 deletions ticket 11a measured, and
#   the only reason String.wz was the one file in the composed set that subtracted. The names still
#   land; only the granularity changed. Same narrowing in COLLISION-FORCE.txt.
#
# Eqp/Dragon - one collapsed force ROOT covering 12 Evan dragon-equip names (Silver/Gold/Reverse
#   Mask, Pendant, Wings, Tail). OWNERSHIP CALL, made here rather than left ambiguous: these are
#   Evan equips, not mounts, so on subject matter they read as ticket 13's. They are 05's anyway,
#   because (a) ticket 04 already shipped a String.wz path list that excludes them and names 05 as
#   the owner, (b) ticket 13 is hard-blocked behind ticket 01's human client launch and has not
#   started, and (c) the container is id-for-id identical live vs v84 (12 ids, 1942000/01/02,
#   1952000/01/02, 1962000/01/02, 1972000/01/02 - verified by dump on both sides), so forcing the
#   whole root is lossless. TICKET 13 MUST NOT RE-ADD THIS ROW - it would be a double force.
#
# Merge: WzMerge merge <v84>\String.wz <pre>\String.wz <out>\String.wz THIS <conflicts> --deny <lists>\COLLISION-DENY.txt --force <lists>\COLLISION-FORCE.txt --live D:\games\MapleStory\String.wz
# XML:   WzMerge xml   <v84>\String.wz wz THIS <conflicts> --deny <lists>\COLLISION-DENY.txt --force <lists>\COLLISION-FORCE.txt
#
# ---------------------------------------------------------------------------------------------
# TAKEN BY TICKET 03f, 2026-08-16 -> ..\03f\String.paths.txt (27 rows, expanded and merged).
# They are ALSO on ..\composed\String.paths.txt. Do not paste them anywhere a third time.
#
# The 27 String.wz/Skill.img rows that name these mount skills. Pure additions (absent from the
# live client, zero collisions), so no --force. 05's ownership was scoped to Eqp/Taming +
# Eqp/Dragon, so they were left here ready to paste rather than merged. Evan's copies
# (20011025-20011039) and the Aran/Dragon rows on the same add-list are ticket 12/13's and are
# still NOT listed - keep that exclusion.
#   String.wz/Skill.img/{0001025,0001026,0001027,0001028,0001029,0001030,0001037,0001038,0001039}
#   String.wz/Skill.img/{10001025,10001026,10001027,10001028,10001029,10001030,10001037,10001038,10001039}
#   String.wz/Skill.img/{20001025,20001026,20001027,20001028,20001029,20001030,20001037,20001038,20001039}
#
# CORRECTION (03f). This header used to describe the gap as cosmetic - "the nine skills appear in
# the client skill window with no name and no description". It is worse than that:
# String.wz/Skill.img is the ENUMERATION SOURCE the server grants skills from
# (MaxSkillCommand.java:44, ResetSkillCommand.java:44, NPCConversationManager.java:395 all iterate
# getData("Skill.img").getChildren() -> SkillFactory.getSkill(parseInt(name))). A skill absent
# from Skill.img is never visited, so !maxskill skipped exactly these nine - and there is no
# !skill command in this codebase. Without these rows the eight mounts were unobtainable.
# ---------------------------------------------------------------------------------------------
String.wz/Eqp.img/Eqp/Dragon
String.wz/Eqp.img/Eqp/Taming/1902040/name
String.wz/Eqp.img/Eqp/Taming/1902041/name
String.wz/Eqp.img/Eqp/Taming/1902042/name
String.wz/Eqp.img/Eqp/Taming/1912033/name
String.wz/Eqp.img/Eqp/Taming/1912034/name
String.wz/Eqp.img/Eqp/Taming/1912035/name

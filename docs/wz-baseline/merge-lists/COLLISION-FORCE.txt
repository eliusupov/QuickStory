# Force-list: node paths the operator EXPLICITLY authorises the merge to overwrite in the
# live client, contrary to the additive-only rule. Produced by ticket 03c; rationale and the
# full 735-row triage are in COLLISION-TRIAGE.md next to this file.
#
# ENFORCED since ticket 03e: pass this file as `WzMerge merge|xml --force <file>`. It is the
# ONLY way past the additive-only gate; without it no existing node can be overwritten. Optional,
# unlike --deny, because "overwrite nothing" is a safe default and "deny nothing" is not.
#
# FORMAT (identical to COLLISION-DENY.txt; one parser serves both):
#   <path>\t# <reason>
# - blank lines and lines whose first non-space char is '#' are ignored
# - <path> is the same "Wz.wz/rest/of/path" form the add-lists and conflicts.txt use
# - a listed path is a force ROOT: everything under it is overwritten too
# - a path NOT listed here keeps its live value; this file has no "deny" syntax and needs none
#
# THIS FILE HAS A COUNTERPART. COLLISION-DENY.txt lists paths the merge must refuse even
# though they are ABSENT from the target — additions the gate cannot catch, because the gate
# only guards overwrites. Same format, so one parser serves both. If a path somehow appears in
# both files: deny wins, and it should be a hard exit — an overlap is an operator error worth
# stopping for, not something to resolve silently.
#
# GRANULARITY (ticket 03k). 18 rows below name `<id>/name`, not `<id>`. A force root is a ROOT:
# the tool removes everything at or beneath it and puts v84's subtree in its place. For those 18
# ids v84's node carries ONLY `name`, while the live client's carries `name` AND `desc`, so an
# id-level root deleted the live `desc` as a side effect of fixing the name. Ticket 11a measured
# it: 18 `desc` nodes removed, making String.wz the only file in the composed set that subtracts.
# Narrowing the root to `/name` keeps the fix and keeps the `desc`. The other 19 rows stay at id
# level because v84 supplies the whole node there and nothing is lost - measured, not assumed.
# The same 18 rows are narrowed in ..\04\String.paths.txt and ..\05\String.paths.txt.
#
# Every row below was verified three ways (v83-stock / v84 / live client) before listing.
# The whole list is one rule: the live value is the string literal "MISSING NAME" /
# "MISSING INFO", i.e. a Cosmic name-table stub, and v84 has the real text.
# Nothing here is Cosmic custom content. 37 rows.

String.wz/Cash.img/5500005	# live "MISSING NAME" -> v84 "[50days] Magical Sandglass"
String.wz/Cash.img/5500006	# live "MISSING NAME" -> v84 "[99days] Magical Sandglass"
String.wz/Cash.img/5530001	# live "MISSING NAME" -> v84 "DS Medal Basket"
String.wz/Cash.img/5530002	# live "MISSING NAME" -> v84 "5 Golden Compasses"
String.wz/Cash.img/5530003	# live "MISSING NAME" -> v84 "10 Gaga Transformation Potions"
String.wz/Cash.img/5530004	# live "MISSING NAME" -> v84 "100 Elixirs"
String.wz/Cash.img/5530005	# live "MISSING NAME" -> v84 "200 Elixirs"
String.wz/Cash.img/5530006	# live "MISSING NAME" -> v84 "100 Power Elixirs"
String.wz/Cash.img/5530007	# live "MISSING NAME" -> v84 "150 Power Elixirs"
String.wz/Cash.img/5530008	# live "MISSING NAME" -> v84 "200 Power Elixirs"
String.wz/Consume.img/2100166	# live "MISSING NAME" -> v84 "Free Spirit Summoning Sack"
String.wz/Eqp.img/Eqp/Accessory/1142143/name	# live "MISSING NAME" -> v84 name is untranslated KMS text; still better than a stub
String.wz/Eqp.img/Eqp/Accessory/1142144/name	# live "MISSING NAME" -> v84 "DS Mania Medal"
String.wz/Eqp.img/Eqp/Accessory/1142145/name	# live "MISSING NAME" -> v84 name is untranslated KMS text
String.wz/Eqp.img/Eqp/Accessory/1142149/name	# live "MISSING NAME" -> v84 name is untranslated KMS text
String.wz/Eqp.img/Eqp/Accessory/1142150/name	# live "MISSING NAME" -> v84 name is untranslated KMS text
String.wz/Eqp.img/Eqp/Accessory/1142151/name	# live "MISSING NAME" -> v84 name is untranslated KMS text
String.wz/Eqp.img/Eqp/Dragon	# 12 Evan dragon-equip names, all live "MISSING NAME" -> Silver/Gold/Reverse Mask,Pendant,Wings,Tail. Ticket 04-09 blocker.
String.wz/Eqp.img/Eqp/Glove/1082262/name	# live "MISSING NAME" -> v84 "Dragon Master's Proof"
String.wz/Eqp.img/Eqp/Longcoat/1051176/name	# live "MISSING NAME" -> v84 name is untranslated KMS text
String.wz/Eqp.img/Eqp/Longcoat/1052217/name	# live "MISSING NAME" -> v84 "Cloud Robe"
String.wz/Eqp.img/Eqp/Longcoat/1052224/name	# live "MISSING NAME" -> v84 "Strawberry Baby"
String.wz/Eqp.img/Eqp/Longcoat/1052228/name	# live "MISSING NAME" -> v84 "Layered Long Skull Tee"
String.wz/Eqp.img/Eqp/PetEquip/1802039	# live "MISSING NAME" -> v84 "Golden Pig Lucky Sack"
String.wz/Eqp.img/Eqp/Shield/1092067/name	# live "MISSING NAME" -> v84 "Transparent Shield"
String.wz/Eqp.img/Eqp/Taming/1902040/name	# live "MISSING NAME" -> v84 "Stage 1 Dragon". Evan mount, ticket 04-09 blocker.
String.wz/Eqp.img/Eqp/Taming/1902041/name	# live "MISSING NAME" -> v84 "Stage 2 Dragon"
String.wz/Eqp.img/Eqp/Taming/1902042/name	# live "MISSING NAME" -> v84 "Stage 3 Dragon"
String.wz/Eqp.img/Eqp/Taming/1912033/name	# live "MISSING NAME" -> v84 "Stage 1 Dragon Saddle"
String.wz/Eqp.img/Eqp/Taming/1912034/name	# live "MISSING NAME" -> v84 "Stage 2 Dragon Saddle"
String.wz/Eqp.img/Eqp/Taming/1912035/name	# live "MISSING NAME" -> v84 "Stage 3 Dragon Saddle"
String.wz/Eqp.img/Eqp/Weapon/1452058	# live "MISSING NAME" -> v84 "Barisetter"
String.wz/Etc.img/Etc/4161049	# live "MISSING NAME" -> v84 "Dragon Types and Characteristics (Vol.I)". Evan quest item.
String.wz/Etc.img/Etc/4161050	# live "MISSING NAME" -> v84 "Dragon Types and Characteristics (Vol.II)". Evan quest item.
String.wz/Etc.img/Etc/4161051	# live "MISSING NAME" -> v84 "Crew Member's Voyage Log". Evan quest item.
String.wz/Ins.img/3994179	# live "MISSING NAME" -> v84 name is untranslated KMS text
String.wz/Ins.img/3994180	# live "MISSING NAME" -> v84 name is untranslated KMS text

# ---------------------------------------------------------------------------------------
# NOT AUTHORISED — the 4 remaining ambiguous rows. Default is keep-local (absent from the list
# above). Uncomment only after the owner rules; see COLLISION-TRIAGE.md "Ambiguous".
#
# Character.wz/Dragon/01942002.img/info/level	# adopt = real Evan growth curve, but see below
# Character.wz/Dragon/01952002.img/info/level	# ditto
# Character.wz/Dragon/01962002.img/info/level	# ditto
# Character.wz/Dragon/01972002.img/info/level	# ditto
#
# Not a display-only change: ItemInformationProvider.java:315-322 blind-scans Character.wz
# subdirectories for "0"+itemId+".img", so it reads these as generic EQUIP levelling and lands
# on Dragon/ incidentally. Live's single-child rows are inert (level 1, early return); v84's
# rows switch that dormant path on for four items. Decide the behaviour, then set this.
#
# Etc.wz/Commodity.img/894{1..6} were ambiguous and are now SETTLED: keep local, zero loss.
# CashShop.java:238-249 keys the map by SN and never reads the node name, and v84's six entries
# already exist in both trees as nodes 8848-8853 (same SNs 70000365-70000370). Forcing them
# would duplicate six SNs and delete the six live pet SNs 60001000-60001005. Do not add them.
